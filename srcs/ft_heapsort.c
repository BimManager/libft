#if 0

ft_heapsort.c

#endif

#include "libft.h"

int	ft_heapsort(void *base, size_t nel,
		size_t width, int (*compar)(const void *, const void *))
{
  (void)base;
  (void)nel;
  (void)width;
  (void)compar;
  return (0);
}
